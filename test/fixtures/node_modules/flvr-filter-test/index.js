#!/usr/bin/env node
'use strict'

/* -----------------------------------------------------------------------------
 * filter
 * -------------------------------------------------------------------------- */

module.exports = function (_err, frames) {
  return frames.filter((frame) => frame.getLineNumber() === 18)
}
